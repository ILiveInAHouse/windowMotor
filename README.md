# windowMotor
Motorized Windows: with position and torque monitor

```yaml
# example configuration:

external_components:
  - source:
      type: local
      path: ../components

windowMotor:
  boardid0_pin: GPIO18
  boardid1_pin: GPIO43
  boardid2_pin: GPIO33
```